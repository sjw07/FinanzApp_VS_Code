<Window x:Class="FinanzApp.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:FinanzApp.Converters"
        Title="FinanzApp" Height="450" Width="400">
    <Window.Resources>
        <converters:AmountToColorConverter x:Key="AmountToColorConverter" />
    </Window.Resources>
    <Grid>
        <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" IsReadOnly="True" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Date, StringFormat=dd.MM}" Header="Datum" Width="80"/>
                <DataGridTextColumn Binding="{Binding Name}" Header="Name" Width="*"/>
                <DataGridTextColumn Binding="{Binding Amount, StringFormat=F2}" Header="Betrag (€)" Width="90">
        <converters:AmountToBrushConverter x:Key="AmountToBrushConverter" />
    </Window.Resources>
    <Grid>
        <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" Margin="10" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Datum" Binding="{Binding Date, StringFormat=dd.MM}" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                <DataGridTextColumn Header="Betrag (€)">
                    <DataGridTextColumn.Binding>
                        <Binding Path="Amount" StringFormat="F2 €" />
                    </DataGridTextColumn.Binding>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{Binding Amount, Converter={StaticResource AmountToBrushConverter}}" />
        xmlns:viewModels="clr-namespace:FinanzApp.ViewModels"
        xmlns:conv="clr-namespace:FinanzApp.Converters"
        Title="Mai 2025" Height="450" Width="400">
    <Window.Resources>
        <conv:AmountToColorConverter x:Key="AmountToColorConverter" />
    </Window.Resources>
    <Grid>
        <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False"
                  HorizontalAlignment="Center" VerticalAlignment="Center"
                  Width="350">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Date, StringFormat=dd.MM}" Header="Datum" Width="120"/>
                <DataGridTextColumn Binding="{Binding Name}" Header="Name" Width="*"/>
                <DataGridTextColumn Header="Betrag (€)" Width="90">
                    <DataGridTextColumn.Binding>
                        <Binding Path="Amount" StringFormat="{}{0:F2} €" />
                    </DataGridTextColumn.Binding>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{Binding Amount, Converter={StaticResource AmountToColorConverter}}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
