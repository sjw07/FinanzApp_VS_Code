<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FinanzApp.YearView">
    <Grid>
        <Image Source="backgroundimage.png" Aspect="AspectFill" Opacity="0.5" />
        <VerticalStackLayout>
            <Label Text="Jahresübersicht" FontAttributes="Bold" FontSize="18" HorizontalOptions="Center" Margin="0,10" />
            <Grid ColumnDefinitions="3*,1*" ColumnSpacing="10" VerticalOptions="Fill">
                <Border Grid.Column="0" Margin="40,0,0,0" Stroke="Black" StrokeThickness="2" BackgroundColor="#0DD3D3D3">
                    <ScrollView x:Name="Scroll" Orientation="Both">
                        <Grid x:Name="YearGrid" Padding="3" RowSpacing="2" ColumnSpacing="5" />
                    </ScrollView>
                </Border>
                <VerticalStackLayout Grid.Column="1" Spacing="10" VerticalOptions="Center">
                    <Button Text="Home" Clicked="OnHomeClicked" TextColor="White" FontAttributes="Bold" WidthRequest="200" />
                    <Button Text="Abmelden" Clicked="OnLogoutClicked" BackgroundColor="Red" TextColor="White" FontAttributes="Bold" WidthRequest="200" />
                </VerticalStackLayout>
            </Grid>
        </VerticalStackLayout>
    </Grid>
</ContentPage>
