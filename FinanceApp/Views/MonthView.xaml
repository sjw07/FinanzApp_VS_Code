<Window x:Class="FinanceApp.Views.MonthView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mai 2025" Height="500" Width="400">
    <Grid>
        <ListView ItemsSource="{Binding Items}" Width="350" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnDefinitions="120,*,90" Padding="4">
                        <TextBlock Grid.Column="0" Text="{Binding Date, StringFormat=dd.MM}" />
                        <TextBlock Grid.Column="1" Text="{Binding Name}" />
                        <TextBlock Grid.Column="2" Text="{Binding Amount, StringFormat={}{0:F2} €}" Foreground="{Binding Amount, Converter={StaticResource AmountToColorConverter}}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
